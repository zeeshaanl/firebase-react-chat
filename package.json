{
  "name": "firebase-react-chat",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.13"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && surge build/ http://zeeshaan-chat.surge.sh/ && echo The Firebase-React chat app is live at http://zeeshaan-chat.surge.sh/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "firebase": "^4.4.0"
  }
}
